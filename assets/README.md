# Build resources

| File | Purpose |
|------|---------|
| `icon.png` | App icon (512×512). electron-builder generates platform icons from this. |

The same icon is bundled for window title bars and referenced by sample markdown files.
